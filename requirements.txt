# ── Core ─────────────────────────────────────────────────────────────────────
langgraph>=0.2
langchain>=0.3
langchain-core>=0.2
langchain-openai>=0.1
langchain-mcp-adapters>=0.2
openai>=1.30
fastapi>=0.111
uvicorn[standard]>=0.29
pyyaml>=6.0
httpx>=0.27
typing-extensions>=4.11
fastmcp>=2.0

# ── LangGraph checkpointer backends ──────────────────────────────────────────
# In-memory checkpointer is bundled with langgraph (no extra install needed).
# Install ONE of the following based on your memory.type setting:
#
#   memory.type: sqlite   → pip install langgraph-checkpoint-sqlite
#   memory.type: postgres → pip install langgraph-checkpoint-postgres
#
# Uncomment to pre-install:
# langgraph-checkpoint-sqlite>=2.0
# langgraph-checkpoint-postgres>=2.0

# ── OpenTelemetry ─────────────────────────────────────────────────────────────
opentelemetry-sdk>=1.24
opentelemetry-exporter-otlp-proto-http>=1.24   # active: HTTP/protobuf to port 4318
opentelemetry-exporter-otlp-proto-grpc>=1.24   # available if switching to gRPC/port 4317

# ── LLM providers ─────────────────────────────────────────────────────────────
# Gemini
langchain-google-genai>=2.0

# IBM watsonx (uncomment to enable)
# langchain-ibm>=0.3
